services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: family_butler
      POSTGRES_USER: family_butler
      POSTGRES_PASSWORD: family_butler
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
